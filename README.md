# ksefkoszule

Single-file browser app that converts legacy Koszule invoice HTML into KSeF FA(3) XML.

The site is published as a static page and works without a backend. Open `index.html` locally or use the GitHub Pages URL after publishing.
